<p id="notice"><%= notice %></p>

<h1>Listing Seats</h1>


<!-- <div style="margin-top: 200px;">
  <h3>LIPPUJEN VARAAMINEN
  <span class="label label-primary" style="font-size: 110%;">0465734026</span></h3>
</div> -->

<!-- <div class="container container-center"> -->

<%= render partial: "seats_table" %>

  <% if logged_in? && current_user.is_admin %>

    <div class="row">
      <div class="col-xs-offset-2 col-xs-4">
        <%= button_to "Päivittää", update_path, method: 'post', class: 'btn btn-block btn-primary', id: 'btn-update', params: { seats_to_update: "get_seats_arr()" } %>
      </div>
      <div class="col-xs-4">
        <%= button_to "Peruuttaa", root_path, method: 'get', class: 'btn btn-block btn-default' %>
      </div>
    </div>
    <br>

  <% end %>

<!-- </div> -->

<table>
  <thead>
    <tr>
      <th>Seat</th>
      <th>Row</th>
      <th>Column</th>
      <th>Price</th>
      <th>Status</th>
      <th>Flight</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @seats.each do |seat| %>
      <tr>
        <td><%= seat.seat_id %></td>
        <td><%= seat.row %></td>
        <td><%= seat.column %></td>
        <td><%= seat.price %></td>
        <td><%= seat.status %></td>
        <td><%= seat.flight_id %></td>
        <td><%= link_to 'Show', seat %></td>
        <td><%= link_to 'Edit', edit_seat_path(seat) %></td>
        <td><%= link_to 'Destroy', seat, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Seat', new_seat_path %>
